# Réglages Netlify — l'hébergeur du site définitif : il sert Next tel quel
# (runtime Next détecté de lui-même), porte l'administration Decap
# (Identity + Git Gateway, voir GUIDE-CONFIGURATION.md) et collecte le
# formulaire d'infolettre (public/__forms.html).

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
